import { LOGIN, LOGOUT, SUBMIT_FORM } from './types';

const initialState = {
  isLogged: false
};

export const rootReducer = (state, action) => {
  switch(action.type) {
    default: return state;
  }
};
